file(GLOB SOURCES
    "${CMAKE_CURRENT_SOURCE_DIR}/src/*.s"
    "${CMAKE_CURRENT_SOURCE_DIR}/src/*.c"
)

add_library(STM32_USB_FS_Device_Driver STATIC ${SOURCES})

target_include_directories(STM32_USB_FS_Device_Driver PUBLIC 
    ${CMAKE_CURRENT_SOURCE_DIR}/inc
    $<TARGET_PROPERTY:STM32F10x_StdPeriph_Driver,INTERFACE_INCLUDE_DIRECTORIES>
    $<TARGET_PROPERTY:CMSIS,INTERFACE_INCLUDE_DIRECTORIES>    
)